language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-basic' ]; then cd .. && eval "mv $currentfolder generator-basic" && cd generator-basic; fi

